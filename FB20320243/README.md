# .inspector(...) view modifier will sometimes cause the detail view of a NavigationSplitView to layout incorrectly

SwiftUI’s .inspector(...) view modifier will sometimes cause the detail view of a NavigationSplitView to layout incorrectly, as demonstrated in the video where it happens twice.

The issue can be reproduced using the attached sample project.

![](video.mov)
